# 更新记录

本项目遵循语义化版本。发布日期以安装包最终通过发布矩阵的日期为准。

## 0.1.0 - 待发布

### 新增

- 上传一个合并的 TXT 周报和一个 DOCX 模板，识别人员与模板板块。
- 通过 OpenAI-compatible 私有模型提炼个人事实、跨人员聚合并生成 DOCX。
- 人员、模板、逐板块校审、来源追溯、版本恢复和失败步骤重试。
- Windows 服务与 Inno Setup 安装器，以及 Linux systemd、deb 和 tar.gz 安装方式。
- SQLite 迁移备份、启动恢复、结构化脱敏日志、诊断包和 readiness 检查。
- 文件类型、大小和 DOCX ZIP 防护，数据使用量、按任务删除与可选保留期清理。

### 安全默认值

- 仅监听 `127.0.0.1:8765`。
- 模型密钥不由读取接口回传；Windows 使用 DPAPI，Linux 使用权限为 `0600` 的密钥文件。
- 模型请求不继承系统代理，周报正文、模型响应、密钥和绝对路径不进入结构化日志。

### 发布状态

- macOS 开发机自动化测试与 bundle 冒烟：通过（后端 177 项、前端 16 项、浏览器 E2E 2 项）。
- Windows 与 Linux 原生构建、安装、升级、卸载矩阵：未验证，不得据此发布正式版。
